import React from 'react';
import { Layout } from 'antd'; // Sử dụng Layout từ Ant Design
import './appFooter.scss';

const { Footer } = Layout;

const AppFooter = () => {
    return <Footer className="footer">Footer222</Footer>;
};

export default AppFooter;
